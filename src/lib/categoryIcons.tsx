import { BookOpen, Feather, Gem, Ghost, HeartHandshake, Laugh, PawPrint, Shield, Sparkles, Zap } from 'lucide-react';
import type { LucideIcon } from 'lucide-react';
import type { CategoryIcon } from '../config/product';

export const categoryIconMap: Record<CategoryIcon, LucideIcon> = {
  PawPrint,
  Shield,
  Sparkles,
  BookOpen,
  Laugh,
  HeartHandshake,
  Feather,
  Gem,
  Zap,
  Ghost,
};
