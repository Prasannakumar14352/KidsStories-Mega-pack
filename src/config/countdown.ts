/**
 * Length of the launch-offer countdown shown on the announcement bar, hero
 * and pricing sections. Change it here - nowhere else. The timer starts
 * fresh at this duration on every page load; it is display-only urgency and
 * never affects price or checkout.
 */
export const OFFER_DURATION_HOURS = 2;
